@{
    ViewData["Title"] = "Access denied";
}
<div class="card max-w-md"><div class="card-body text-center">
    <i data-lucide="shield-alert" class="text-4xl text-red-500"></i>
    <h2 class="text-xl font-semibold text-red-600 mt-2">Access denied</h2>
    <p class="text-slate-600 mt-1 mb-4">You do not have permission to view this resource.</p>
    <a class="btn-outline" asp-controller="Home" asp-action="Index">Back to home</a>
</div></div>

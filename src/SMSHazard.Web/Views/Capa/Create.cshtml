@model SMSHazard.Web.Models.Capa.CreateCapaViewModel
@{
    ViewData["Title"] = "Assign corrective action";
}
<h2 class="text-xl font-semibold text-slate-900">Assign corrective / preventive action</h2>
<p class="text-slate-500 mb-4">@Model.ReferenceNo — @Model.Title</p>

<div class="card max-w-3xl"><div class="card-body">
    <partial name="_CreateForm" model="Model" />
</div></div>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
